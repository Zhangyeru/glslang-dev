#version 450 core

#extension GL_KHR_memory_scope_semantics : enable
#extension GL_HW_neural_shader : enable
#extension GL_EXT_shader_explicit_arithmetic_types : enable

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(set = 0, binding = 0) buffer InBuf { float16_t data[]; } inBuf;
layout(set = 0, binding = 1) buffer OutBuf { float16_t data[]; } outBuf;

// No matmul use. The opt pass should default this matrix type to MatrixUseAHW.
void main()
{
    const uint offset = 0;
    const uint stride = 16;

    coopmatHW<float16_t, 16, 16> value;

    coopMatLoadHW(value, inBuf.data, offset, stride, gl_CooperativeMatrixLayoutRowMajorHW);
    coopMatStoreHW(value, outBuf.data, offset, stride, gl_CooperativeMatrixLayoutColumnMajorHW);
}
