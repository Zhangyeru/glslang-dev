#version 460 core
#extension GL_EXT_shader_16bit_storage : require
#extension GL_EXT_shader_explicit_arithmetic_types_int16 : require

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(set = 0, binding = 0) buffer InputA { uint16_t data[]; } A;
layout(set = 0, binding = 1) buffer InputB { uint16_t data[]; } B;
layout(set = 0, binding = 2) buffer InputC { uint16_t data[]; } C;
layout(set = 0, binding = 3) buffer OutputD { uint16_t data[]; } D;

void main()
{
    for (int row = 0; row < 4; ++row) {
        for (int col = 0; col < 4; ++col) {
            uint16_t acc = C.data[row * 4 + col];
            for (int inner = 0; inner < 4; ++inner)
                acc += A.data[row * 4 + inner] * B.data[inner * 4 + col];
            D.data[row * 4 + col] = acc;
        }
    }
}

